import React from 'react';
import AuthProviderContext from '../context/AuthProvider';

// Export the AuthProvider component from the context
export const AuthProvider = AuthProviderContext;

// Also export as default for default imports
export default AuthProviderContext; 