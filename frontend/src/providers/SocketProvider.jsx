import React from 'react';
import SocketProviderContext from '../context/SocketProvider';

// Export the SocketProvider component from the context
export const SocketProvider = SocketProviderContext;

// Also export as default for default imports
export default SocketProviderContext; 